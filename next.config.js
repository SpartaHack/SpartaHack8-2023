/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['img.youtube.com', 'image.thum.io', 'lh3.googleusercontent.com'],
    },
  }
  
  module.exports = nextConfig